// Login page — email magic link or OAuth via Supabase Auth
export default function LoginPage() {
  return (
    <main>
      <h1>Sign In</h1>
      {/* TODO: Supabase Auth UI or custom email form */}
    </main>
  );
}
