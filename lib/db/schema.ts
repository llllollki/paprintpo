// Drizzle schema — placeholder file.
// Full schema (products, product_options, quantity_tiers, pricing_rules,
// orders, order_items, order_files) will be defined in Phase 2.

// This file is imported by lib/db/index.ts; it must export at least one symbol.
export const _placeholder = true;
